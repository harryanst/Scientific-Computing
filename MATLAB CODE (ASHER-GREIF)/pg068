x = [];

% Παραγωγή όλων των θετικών αριθμών του συστήματος (2,3,-2,3)
for i = 1:0.25:1.75
	for j = -2:3
		x = [x i*2^j];
	end
end

x = [x -x 0];    % Προσθήκη όλων των αρνητικών αριθμών και του 0
x = sort(x);     %Ταξινόμηση
y = zeros(1,length(x));
plot(x,y,'+')
